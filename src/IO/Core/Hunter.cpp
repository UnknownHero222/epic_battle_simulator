#include "Hunter.hpp"

namespace sw::core {

void Hunter::attack(Unit &targetUnit) {
    //uint32_t distance = std::max(std::abs(targetUnit.getX() - x_), std::abs(targetUnit.getY() - y_));
        // if (distance <= range_) {
        //     //targetUnit.setHp();
        // }
}

}